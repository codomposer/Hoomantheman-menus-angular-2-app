import { readable$_set_ctx_, Readable$, readable$_set_ctx__T } from '@ctx-core/store'
export function ready$_(ready = false):readable$_set_ctx__T<boolean> {
	return readable$_set_ctx_<boolean>(ready)
}
export interface ready$_T extends Readable$<boolean> {}
