import { DEFAULT_LAT, DEFAULT_LNG } from '@menus/web-config'
import type { location_T } from './location_T.js'
export const default_system_location:location_T = Object.freeze(
	{
		lat: DEFAULT_LAT,
		lng: DEFAULT_LNG,
	}
)
