import type { ChartType } from 'chart.js'
// import { ChartType_bubble, ChartType_line } from './ChartType.js'
export function ChartType_(chart_type:ChartType):ChartType {
	// return (chart_type === ChartType_bubble) ? ChartType_line : chart_type
	return chart_type
}
