# NODE.JS server (for Chat APIs)

## Installation
* Install Node.js
* `npm install`

## Development
* run `npm run start` (in seprate terminal)
* run `npm run watch` (in seprate terminal)
* server runs at `http://localhost:8200`